# Set GitLab repositories in `GitAI` object.

Set GitLab repositories in `GitAI` object.

## Usage

``` r
set_gitlab_repos(gitai, ..., verbose = is_verbose())
```

## Arguments

- gitai:

  A `GitAI` object.

- ...:

  Parameters to pass to
  [`set_gitlab_host`](https://r-world-devs.github.io/GitStats/reference/set_gitlab_host.html)
  function.

- verbose:

  A logical. If `FALSE` you won't be getting additional diagnostic
  messages.

## Value

A `GitAI` object.
