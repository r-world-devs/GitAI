# Add files to `GitAI` object.

Add files to `GitAI` object.

## Usage

``` r
add_files(gitai, files)
```

## Arguments

- gitai:

  A `GitAI` object.

- files:

  A character vector of file paths. May be defined with regular
  expression.

## Value

A `GitAI` object.
