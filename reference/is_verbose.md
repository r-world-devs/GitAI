# Checks if `GitAI` is verbose

The function searches for `GITAI_VERBOSE` environmental variable and
tries to read it value as logical.

## Usage

``` r
is_verbose()
```

## Value

A logical. Default is `TRUE`.
